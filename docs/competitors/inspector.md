# MCP Inspector

**Category:** Testing / debug  
**Threat:** High (official, free)  
**URL:** https://github.com/modelcontextprotocol/inspector

## What they do

Visual + CLI tool for listing tools, calling them, debugging MCP servers. Remote server support. Some CI-style programmatic use.

## Gap we exploit

- Manual/debug-first, not a **productized agent-readiness score**
- Protocol interaction, not **realistic multi-step agent tasks**
- No **OpenAPI ? MCP drift** detection
- No **PR-blocking regression** with score history

## Our response

Position mcp-doctor as "Lighthouse for MCP" — static scorecard today, agent eval CI tomorrow. Use Inspector as baseline conformance input, not competitor to fight.
